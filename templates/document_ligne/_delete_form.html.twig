<form method="post" action="{{ path('app_document_ligne_delete', {'id': document_ligne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document_ligne.id) }}">
    <button class="btn">Delete</button>
</form>
