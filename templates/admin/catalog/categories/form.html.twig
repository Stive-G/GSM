{% extends '@EasyAdmin/page/content.html.twig' %}

{% block page_title title %}

{% block content %}
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.slug) }}
    {{ form_row(form.parentId) }}
    {{ form_row(form.position) }}
    {{ form_row(form.isActive) }}
    <button class="btn btn-primary">Enregistrer</button>
    <a class="btn btn-secondary" href="{{ path('admin_catalog_categories_index') }}">Retour</a>
    {{ form_end(form) }}
{% endblock %}
